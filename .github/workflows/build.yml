name: Build Flet App
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout project
        uses: actions/checkout@v4

      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install "flet[all]"

      - name: Accept Android Licenses
        run: |
          yes | flutter doctor --android-licenses

      # Воссоздаем файл ключа из секретов GitHub
      - name: Decode Android Keystore
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > release.jks

      # Команда записана строго в одну строку для защиты от синтаксических ошибок Linux
      - name: Build Android APK
        run: |
          yes | flet build apk --project "my_flet_app" --product "My App" --org "com.example" --build-version "1.0.0" --build-number ${{ github.run_number }} --keystore-path "release.jks" --keystore-password "${{ secrets.KEYSTORE_PASSWORD }}" --keystore-alias "my-key-alias"

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: my-flet-app-apk
          path: build/apk/*.apk
